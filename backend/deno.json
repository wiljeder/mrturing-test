{
  "imports": {
    "hono": "jsr:@hono/hono@^4.7.5"
  },
  "tasks": {
    "dev": "deno run compose:up && deno run --watch --allow-net --allow-env src/main.ts",
    "start": "deno run compose:up && deno run --allow-net --allow-env src/main.ts",
    "compose:up": "docker compose up -d"
  },
  "compilerOptions": {
    "jsx": "precompile",
    "jsxImportSource": "hono/jsx"
  }
}
